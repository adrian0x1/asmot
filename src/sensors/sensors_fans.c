#include "sensors.h"
/* the lm-sensors program doesn't detect any fans on the old computer
that I use as a server. I'll have to implement this at a later time */
int sensors_fans(void) {
    return (-1);
}
